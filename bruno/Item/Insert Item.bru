meta {
  name: Insert Item
  type: http
  seq: 2
}

post {
  url: http://{{host}}:{{port}}/api/{{version}}/{{endpoints.item}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Name",
    "description": "Description",
    "stock": 3
  }
}

settings {
  encodeUrl: true
}
